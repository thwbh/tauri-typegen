export interface {{ name }} {
  {% for field in fields -%}
  {{ field.serializedName | default(value=field.name) }}{% if field.isOptional %}?{% endif %}: {{ field.typescriptType }};
  {% endfor -%}
}
